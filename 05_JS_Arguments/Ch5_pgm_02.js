var msg;
var showMessage;

msg = "It's full of stars!";

showMessage = function () {
    console.log(msg); 
};

showMessage(); 
var msg;
var newMsg;
var showMessage;

msg = "It's full of stars!";
newMsg = "Hello, universe!";

showMessage = function () {
    console.log(msg);
    console.log(newMsg);
};

showMessage(); 

var msg;
var newMsg;
var showMessage;

msg = "It's full of stars!";
newMsg = "Hello, universe!";

showMessage = function () {
    console.log(msg + " " + newMsg);
};

showMessage(); 
